import React from 'react';
import { connect } from 'react-redux';

const Terminal = _ => (
    <div id="terminal">
        <h1>Terminal</h1>
    </div>
);

export default connect(store => ({

}))(Terminal);
