import React from 'react';
import { connect } from 'react-redux';

const Settings = _ => (
    <div id="settings">
        <h1>Settings</h1>
    </div>
);

export default connect(store => ({

}))(Settings);
