import React from 'react';
import { connect } from 'react-redux';

const Gallery = _ => (
    <div id="gallery">
        <h1>Gallery</h1>
    </div>
);

export default connect(store => ({

}))(Gallery);
