import React from 'react';
import { connect } from 'react-redux';

const Dashboard = _ => (
    <div id="dashboard">
        <h1>Dashboard</h1>
    </div>
);

export default connect(store => ({

}))(Dashboard);